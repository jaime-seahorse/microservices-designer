// export enum Roles {
// 	Admin = "admin",
// 	Writter = "writter",
// 	Reader = "reader"
// }

export interface User {
  username: string,
	email: string,
	// role?: Roles
}

// user dto for avatar component
